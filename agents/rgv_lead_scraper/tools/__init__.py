"""Tool modules discovered by OmniAgents via ``rglob('*.py')``.

Marker functions decorated with ``@function_tool`` in the files here are
auto-registered. This ``__init__`` exists only so OmniAgents can resolve
the canonical package name ``rgv_lead_scraper.tools.lead_tools`` rather
than falling back to a synthetic prefix.
"""
